one:
  tenant: one
  name: Primary
  adapter_type: filesystem
  configuration:
    storage_path: "test_storage/one"
  active: true

two:
  tenant: two
  name: Secondary
  adapter_type: s3
  configuration:
    bucket: simple-drive-test
  active: true
